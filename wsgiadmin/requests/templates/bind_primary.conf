{% for domain in domains %}
zone "{{ domain.name }}" IN {
        type master;
        file "pri_auto/{{ domain.name }}.zone";
        allow-query { any; };
        allow-transfer { {{ dns.slave }}; };
        notify yes;
};
{% endfor %}
